let myArr

function addToZero([]) {
    let i = this.i

    
    let sum = for ( 
        
    ) i += i 
        if (sum === 0) {
            return true;
        }
        else {
            return false;
        }
}

addToZero([]);
// -> False

addToZero([1]);
// -> False

addToZero([1, 2, 3]);
// -> False

addToZero([1, 2, 3, -2]);
// -> True